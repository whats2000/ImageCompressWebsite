# Backend for Image Compression Website

## Project Structure
- `src/`: Source code directory
  - `api/`: API route handlers
  - `services/`: Business logic and image processing services
- `tests/`: Unit and integration tests
